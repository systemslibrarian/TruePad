# Android / Gradle build outputs and local machine config — never committed.
.gradle/
build/
*/build/
local.properties
.idea/
*.iml
.kotlin/
captures/
.cxx/
# Signing material must never be committed. The release build is unsigned and no
# signing config exists; these keep a local keystore from ever being added by name.
*.keystore
*.jks
keystore.properties
